import 'react-toastify/dist/ReactToastify.css';

// import type { ToastContent, ToastOptions, ToastPosition } from 'react-toastify';
// import { toast } from 'react-toastify';

// export type UseToastifyProps = {
//   type: string;
//   label: string;
//   args?: ToastOptions;
// };

// export const useToastify = ({ type, label, ...args }: UseToastifyProps) => {
//   if (type) {
//     const toastByType = toast[type as keyof typeof toast];
//     toastByType(label, args);
//   }
// };
