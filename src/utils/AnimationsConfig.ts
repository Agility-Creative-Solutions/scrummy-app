const AnimationsConfig = {
  speed: {
    slow: 0.5,
    medium: 0.4,
    fast: 0.3,
  },
};

const ModalTransition = {
  duration: AnimationsConfig.speed.medium,
};

export { AnimationsConfig, ModalTransition };
